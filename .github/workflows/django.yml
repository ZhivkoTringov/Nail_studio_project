name: Nail_studio

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

    paths-ignore:
      - "*.md"

jobs:
  docker:
    name: Build and Test
    runs-on: ubuntu-latest
    steps:
      - name: Set up environment variables
        run: |
          echo "POSTGRES_USER=postgres" >> $MY_ENV
          echo "POSTGRES_PASSWORD=postgres" >> $MY_ENV
          echo "POSTGRES_DB=postgres" >> $MY_ENV
          echo "POSTGRES_HOST=localhost" >> $MY_ENV
          echo "SECRET_KEY=super secret" >> $MY_ENV
      - uses: actions/checkout@v3
      - uses: iamsauravsharma/create-dotenv@v2.0.1
        with:
          file-path: './.env'
      - uses: isbang/compose-action@v1.5.1
        with:
          compose-file: "./docker-compose.yaml"
          services: |
            nail_studio
            postgres
      - name: Run tests
        run: |
          docker exec nail_studio_project-nail_studio-1 python manage.py test
